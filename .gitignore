# Deno
# https://deno.com/

# Cache directory (when using project-local DENO_DIR)
# https://docs.deno.com/runtime/fundamentals/setup/#deno_dir
.deno/

# Coverage reports
# https://docs.deno.com/runtime/reference/cli/coverage/
coverage/
*.lcov

# Local compile output (deno task compile)
/herdr-mcp

# deno install --vendor / deno2nix local output (must not enter flake src)
vendor/
node_modules/

# Nix build symlink
/result
